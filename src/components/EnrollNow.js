import Link from "next/link";
import React from "react";

const AltLink = () => {
	return (
		<Link href="/" className="py-3 px-6 bg-white text-primary font-bold rounded-[4px]">
			Enroll Now
		</Link>
	);
};

export default AltLink;
